/**
 * @fileoverview wsf-schedule API - fetchScheduleTodayByRoute Tests
 *
 * Tests for the fetchScheduleTodayByRoute endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchScheduleTodayByRoute endpoint
createEndpointSuite("wsf-schedule.fetchScheduleTodayByRoute");
