/**
 * @fileoverview wsf-schedule API - fetchScheduleAlerts Tests
 *
 * Tests for the fetchScheduleAlerts endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchScheduleAlerts endpoint
createEndpointSuite("wsf-schedule.fetchScheduleAlerts");
