/**
 * @fileoverview wsf-vessels API - fetchVesselHistories Tests
 *
 * Tests for the fetchVesselHistories endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-vessels API - fetchVesselHistories endpoint
createEndpointSuite("wsf-vessels.fetchVesselHistories");
