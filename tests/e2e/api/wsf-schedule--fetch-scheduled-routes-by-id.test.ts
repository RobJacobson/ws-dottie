/**
 * @fileoverview wsf-schedule API - fetchScheduledRoutesById Tests
 *
 * Tests for the fetchScheduledRoutesById endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchScheduledRoutesById endpoint
createEndpointSuite("wsf-schedule.fetchScheduledRoutesById");
