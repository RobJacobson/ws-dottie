/**
 * @fileoverview wsf-terminals API - fetchTerminalSailingSpace Tests
 *
 * Tests for the fetchTerminalSailingSpace endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-terminals API - fetchTerminalSailingSpace endpoint
createEndpointSuite("wsf-terminals.fetchTerminalSailingSpace");
