/**
 * @fileoverview wsf-terminals API - fetchTerminalTransportsByTerminalId Tests
 *
 * Tests for the fetchTerminalTransportsByTerminalId endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-terminals API - fetchTerminalTransportsByTerminalId endpoint
createEndpointSuite("wsf-terminals.fetchTerminalTransportsByTerminalId");
