/**
 * @fileoverview wsf-terminals API - fetchTerminalSailingSpaceByTerminalId Tests
 *
 * Tests for the fetchTerminalSailingSpaceByTerminalId endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-terminals API - fetchTerminalSailingSpaceByTerminalId endpoint
createEndpointSuite("wsf-terminals.fetchTerminalSailingSpaceByTerminalId");
