/**
 * @fileoverview wsdot-traffic-flow API - fetchTrafficFlows Tests
 *
 * Tests for the fetchTrafficFlows endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-traffic-flow API - fetchTrafficFlows endpoint
createEndpointSuite("wsdot-traffic-flow.fetchTrafficFlows");
