/**
 * @fileoverview wsdot-weather-readings API - fetchSurfaceMeasurements Tests
 *
 * Tests for the fetchSurfaceMeasurements endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-weather-readings API - fetchSurfaceMeasurements endpoint
createEndpointSuite("wsdot-weather-readings.fetchSurfaceMeasurements");
