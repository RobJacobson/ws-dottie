/**
 * @fileoverview wsdot-highway-cameras API - fetchHighwayCameras Tests
 *
 * Tests for the fetchHighwayCameras endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-highway-cameras API - fetchHighwayCameras endpoint
createEndpointSuite("wsdot-highway-cameras.fetchHighwayCameras");
