/**
 * @fileoverview wsdot-highway-alerts API - fetchMapAreas Tests
 *
 * Tests for the fetchMapAreas endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-highway-alerts API - fetchMapAreas endpoint
createEndpointSuite("wsdot-highway-alerts.fetchMapAreas");
