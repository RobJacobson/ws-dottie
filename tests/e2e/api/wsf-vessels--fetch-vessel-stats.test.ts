/**
 * @fileoverview wsf-vessels API - fetchVesselStats Tests
 *
 * Tests for the fetchVesselStats endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-vessels API - fetchVesselStats endpoint
createEndpointSuite("wsf-vessels.fetchVesselStats");
