/**
 * @fileoverview wsf-schedule API - fetchActiveSeasons Tests
 *
 * Tests for the fetchActiveSeasons endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchActiveSeasons endpoint
createEndpointSuite("wsf-schedule.fetchActiveSeasons");
