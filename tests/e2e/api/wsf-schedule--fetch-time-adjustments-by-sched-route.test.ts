/**
 * @fileoverview wsf-schedule API - fetchTimeAdjustmentsBySchedRoute Tests
 *
 * Tests for the fetchTimeAdjustmentsBySchedRoute endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchTimeAdjustmentsBySchedRoute endpoint
createEndpointSuite("wsf-schedule.fetchTimeAdjustmentsBySchedRoute");
