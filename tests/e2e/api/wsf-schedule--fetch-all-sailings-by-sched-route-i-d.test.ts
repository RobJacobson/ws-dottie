/**
 * @fileoverview wsf-schedule API - fetchAllSailingsBySchedRouteID Tests
 *
 * Tests for the fetchAllSailingsBySchedRouteID endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchAllSailingsBySchedRouteID endpoint
createEndpointSuite("wsf-schedule.fetchAllSailingsBySchedRouteID");
