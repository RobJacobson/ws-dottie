/**
 * @fileoverview wsdot-weather-readings API - fetchWeatherReadings Tests
 *
 * Tests for the fetchWeatherReadings endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-weather-readings API - fetchWeatherReadings endpoint
createEndpointSuite("wsdot-weather-readings.fetchWeatherReadings");
