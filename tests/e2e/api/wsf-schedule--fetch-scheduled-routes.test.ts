/**
 * @fileoverview wsf-schedule API - fetchScheduledRoutes Tests
 *
 * Tests for the fetchScheduledRoutes endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchScheduledRoutes endpoint
createEndpointSuite("wsf-schedule.fetchScheduledRoutes");
