/**
 * @fileoverview wsf-schedule API - fetchRouteDetailsByTripDate Tests
 *
 * Tests for the fetchRouteDetailsByTripDate endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchRouteDetailsByTripDate endpoint
createEndpointSuite("wsf-schedule.fetchRouteDetailsByTripDate");
