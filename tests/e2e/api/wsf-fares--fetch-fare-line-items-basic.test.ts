/**
 * @fileoverview wsf-fares API - fetchFareLineItemsBasic Tests
 *
 * Tests for the fetchFareLineItemsBasic endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-fares API - fetchFareLineItemsBasic endpoint
createEndpointSuite("wsf-fares.fetchFareLineItemsBasic");
