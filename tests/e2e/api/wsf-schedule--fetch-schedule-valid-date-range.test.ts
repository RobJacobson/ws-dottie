/**
 * @fileoverview wsf-schedule API - fetchScheduleValidDateRange Tests
 *
 * Tests for the fetchScheduleValidDateRange endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchScheduleValidDateRange endpoint
createEndpointSuite("wsf-schedule.fetchScheduleValidDateRange");
