/**
 * @fileoverview wsf-terminals API - fetchTerminalTransports Tests
 *
 * Tests for the fetchTerminalTransports endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-terminals API - fetchTerminalTransports endpoint
createEndpointSuite("wsf-terminals.fetchTerminalTransports");
