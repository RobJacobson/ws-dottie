/**
 * @fileoverview wsdot-weather-information API - fetchCurrentWeatherForStations Tests
 *
 * Tests for the fetchCurrentWeatherForStations endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-weather-information API - fetchCurrentWeatherForStations endpoint
createEndpointSuite("wsdot-weather-information.fetchCurrentWeatherForStations");
