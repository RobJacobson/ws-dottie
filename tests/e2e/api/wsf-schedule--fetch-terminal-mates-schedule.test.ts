/**
 * @fileoverview wsf-schedule API - fetchTerminalMatesSchedule Tests
 *
 * Tests for the fetchTerminalMatesSchedule endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchTerminalMatesSchedule endpoint
createEndpointSuite("wsf-schedule.fetchTerminalMatesSchedule");
