/**
 * @fileoverview wsf-terminals API - fetchTerminalWaitTimes Tests
 *
 * Tests for the fetchTerminalWaitTimes endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-terminals API - fetchTerminalWaitTimes endpoint
createEndpointSuite("wsf-terminals.fetchTerminalWaitTimes");
