/**
 * @fileoverview wsf-schedule API - fetchTimeAdjustmentsByRoute Tests
 *
 * Tests for the fetchTimeAdjustmentsByRoute endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchTimeAdjustmentsByRoute endpoint
createEndpointSuite("wsf-schedule.fetchTimeAdjustmentsByRoute");
