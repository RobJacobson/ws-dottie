/**
 * @fileoverview wsf-vessels API - fetchVesselLocations Tests
 *
 * Tests for the fetchVesselLocations endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-vessels API - fetchVesselLocations endpoint
createEndpointSuite("wsf-vessels.fetchVesselLocations");
