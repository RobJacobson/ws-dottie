/**
 * @fileoverview wsdot-weather-readings API - fetchSubSurfaceMeasurements Tests
 *
 * Tests for the fetchSubSurfaceMeasurements endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-weather-readings API - fetchSubSurfaceMeasurements endpoint
createEndpointSuite("wsdot-weather-readings.fetchSubSurfaceMeasurements");
