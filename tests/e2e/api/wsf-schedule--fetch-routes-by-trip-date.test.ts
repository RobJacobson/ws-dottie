/**
 * @fileoverview wsf-schedule API - fetchRoutesByTripDate Tests
 *
 * Tests for the fetchRoutesByTripDate endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsf-schedule API - fetchRoutesByTripDate endpoint
createEndpointSuite("wsf-schedule.fetchRoutesByTripDate");
