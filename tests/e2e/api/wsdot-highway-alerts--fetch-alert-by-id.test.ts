/**
 * @fileoverview wsdot-highway-alerts API - fetchAlertById Tests
 *
 * Tests for the fetchAlertById endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-highway-alerts API - fetchAlertById endpoint
createEndpointSuite("wsdot-highway-alerts.fetchAlertById");
