/**
 * @fileoverview wsdot-mountain-pass-conditions API - fetchMountainPassConditions Tests
 *
 * Tests for the fetchMountainPassConditions endpoint using the new per-endpoint architecture.
 */

import { createEndpointSuite } from "../shared/api-test-factory";

// Create the complete test suite for wsdot-mountain-pass-conditions API - fetchMountainPassConditions endpoint
createEndpointSuite("wsdot-mountain-pass-conditions.fetchMountainPassConditions");
