/**
 * @fileoverview JSON Parsing Tests
 *
 * This test file runs tests for the JSON parsing utility with fallback mechanisms.
 * It verifies that the utility can handle valid JSON, JSON5-compatible syntax,
 * and malformed JSON with appropriate fallback strategies.
 */

import "./shared/helpers/json-parsing-test";
