/**
 * Response processing utilities for the data pipeline
 *
 * This module handles the third stage of the data pipeline:
 * - Response validation against output schemas
 * - Error handling and transformation
 * - Final data preparation for the client
 */

export { handlePipelineError } from "./handlePipelineError";
