meta {
  name: GetTerminalCombo
 type: http
 seq: 1
}

get {
  url: {{wsf_fares_base_url}}/terminalCombo?api_access_code={{api_access_token}}
 body: none
 auth: inherit
}

params:query {
 api_access_code: {{api_access_token}}
}