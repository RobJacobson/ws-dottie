get {
  url: "{{wsf_fares_base_url}}/cacheflushdate"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
  }
}

description: "Returns the date and time when the WSF fares data was last updated. This operation helps applications coordinate caching of fares data that changes infrequently. When the returned date changes, applications should refresh their cached data. Data updates infrequently."

---