get {
  url: "{{wsf_terminals_base_url}}/terminalVerbose"
 auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
  }
}

description: "Returns a list of TerminalVerbose data for all terminals."

---

get {
  url: "{{wsf_terminals_base_url}}/terminalVerbose/{{terminal_id}}"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns TerminalVerbose data for the terminal with the given identifier."

---