get {
  url: "{{wsf_terminals_base_url}}/cacheflushdate"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns the cache flush date for the terminals API."

---