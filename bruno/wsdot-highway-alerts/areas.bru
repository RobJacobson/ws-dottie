get {
  url: "{{wsdot_highway_alerts_base_url}}/getMapAreasAsJson"
  auth {
    type: query
    params {
      AccessCode: "{{api_access_token}}"
    }
  }
}

description: "Returns a list of map areas for highway alerts."

---