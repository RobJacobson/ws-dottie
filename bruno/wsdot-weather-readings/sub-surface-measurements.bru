get {
  url: "{{wsdot_weather_readings_base_url}}/Scanweb/SubSurfaceMeasurements"
  auth {
    type: query
    params {
      AccessCode: "{{api_access_token}}"
    }
  }
}

description: "Returns sub-surface measurements from all weather stations."

---