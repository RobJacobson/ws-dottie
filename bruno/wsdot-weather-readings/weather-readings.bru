get {
  url: "{{wsdot_weather_readings_base_url}}/Scanweb"
  auth {
    type: query
    params {
      AccessCode: "{{api_access_token}}"
    }
  }
}

description: "Returns current weather readings from all weather stations."

---