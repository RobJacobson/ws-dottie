get {
  url: "{{wsdot_weather_stations_base_url}}/GetCurrentStationsAsJson"
  auth {
    type: query
    params {
      AccessCode: "{{api_access_token}}"
    }
  }
}

description: "Returns current weather station data for all stations."

---