get {
  url: "{{wsf_schedule_base_url}}/routeshavingservicedisruptions/{{trip_date}}"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
  }
}

description: "Returns service disruptions for routes on the specified trip date."

---