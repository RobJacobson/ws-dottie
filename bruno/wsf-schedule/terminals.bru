get {
  url: "{{wsf_schedule_base_url}}/terminals"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns a list of all terminals."

---

get {
  url: "{{wsf_schedule_base_url}}/terminalsandmates"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns terminals and their mate information."

---