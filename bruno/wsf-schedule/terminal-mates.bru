get {
  url: "{{wsf_schedule_base_url}}/terminmates"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns terminal mate information."

---

get {
  url: "{{wsf_schedule_base_url}}/terminmatesbyroute/{route_id}"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns terminal mate information for the specified route."

---