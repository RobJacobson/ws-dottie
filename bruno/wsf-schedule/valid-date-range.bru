get {
  url: "{{wsf_schedule_base_url}}/validaterange"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns the valid date range for schedules."

---