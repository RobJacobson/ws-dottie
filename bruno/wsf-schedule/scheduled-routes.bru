get {
  url: "{{wsf_schedule_base_url}}/scheduledroutes"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns all scheduled routes."

---

get {
  url: "{{wsf_schedule_base_url}}/scheduledroutesbyseason/{scheduled_season_id}"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns scheduled routes for the specified scheduled season ID."

---