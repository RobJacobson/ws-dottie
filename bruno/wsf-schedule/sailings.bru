get {
  url: "{{wsf_schedule_base_url}}/allsailings/{sched_route_id}"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns all sailing data for the specified scheduled route ID."

---

get {
  url: "{{wsf_schedule_base_url}}/sailings/{sched_route_id}"
  auth {
    type: query
    params {
      api_access_code: "{{api_access_token}}"
    }
 }
}

description: "Returns sailing data for the specified scheduled route ID."

---