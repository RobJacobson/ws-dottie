get {
  url: "{{wsdot_border_crossings_base_url}}/GetBorderCrossingsAsJson"
  auth {
    type: query
    params {
      AccessCode: "{{api_access_token}}"
    }
  }
}

description: "Returns current border crossing wait time data for all border crossings."

---