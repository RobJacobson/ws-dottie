get {
  url: "{{wsdot_weather_information_base_url}}/getWeatherInformationAsJson"
  auth {
    type: query
    params {
      AccessCode: "{{api_access_token}}"
    }
  }
}

description: "Returns weather information from weather stations."

---